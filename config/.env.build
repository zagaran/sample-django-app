# Env file for the purposes of building the docker image; these are NOT persisted in the image
DATABASE_URL=sqlite:///db.sqlite3 # DB needs to be set to run management commands
BUILD=True
PRODUCTION=False
SECRET_KEY=
GOOGLE_OAUTH2_KEY=
GOOGLE_OAUTH2_SECRET=
DEFAULT_FROM_EMAIL=
